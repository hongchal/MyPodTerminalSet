# Adopted Skills

## mattpocock/skills 유래 생산성 스킬 (user-global)
- 소스: `dotfiles/profile/skills/` → `~/.claude/skills/` symlink 배포 (55-claude-profile)
- 도입 목록 (6개): `grill-me`, `grill-with-docs`, `tdd`, `to-prd`, `to-issues`, `improve-codebase-architecture`
- 용도: 구현 전 plan grilling(`grill-me`/`grill-with-docs`) → PRD/이슈 분해(`to-prd`/`to-issues`) → TDD 구현(`tdd`) → 구조 개선(`improve-codebase-architecture`)
- 버전: qwen3-nvfp4-quantize에서 검증된 self-contained 버전 (upstream 최신과 다름 — upstream은 `/grilling`, `setup-matt-pocock-skills` 의존)
- 신규 도입 절차: `dotfiles/profile/skills/README.md` 참조
